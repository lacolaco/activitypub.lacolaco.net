{
  "name": "activitypub-lacolaco-net",
  "private": true,
  "scripts": {
    "format": "prettier --cache --write .",
    "build:server": "docker build ."
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
